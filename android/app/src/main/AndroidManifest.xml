<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        ACCESS_NETWORK_STATE: needed for the wireless-debugging IP
        readout on the status overlay (ConnectivityManager.allNetworks
        / getLinkProperties). Normal protection level — granted at
        install time, no runtime prompt. Also unblocks
        NetworkInterface.getNetworkInterfaces() on Android 14+, which
        returns null when the caller has zero network permissions.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:label="Penflow"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="userLandscape"
            android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen"
            android:resizeableActivity="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
